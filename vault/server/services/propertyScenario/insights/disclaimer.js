'use strict';

/**
 * Locked disclaimer for document-insight results.
 * Must never be paraphrased away — tests assert the literal string.
 */
const INSIGHT_DISCLAIMER =
  'AI-generated analysis of publicly available lender documents — not financial or legal advice, '
  + 'and not a substitute for confirming terms directly with the lender.';

module.exports = { INSIGHT_DISCLAIMER };
